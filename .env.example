# Copy this file to `.env` and provide the connection string for your AWS RDS instance.
# Replace the placeholder below with your actual DATABASE_URL (e.g. from your secret manager).
DATABASE_URL="<DATABASE_URL>"
