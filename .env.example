# This template documents the environment variables expected by the tooling.
# Copy it to `.env` only if you need a local override—otherwise rely on the
# `DATABASE_URL` value that is already provided by your runtime (for example,
# the Vercel project-level secret) or secret manager so the secret never
# touches version control. Nothing in the app reads from `.env.example` itself.

# Prisma uses this connection string to reach the database. Keep the actual
# value in your secure store and export it before running commands, e.g.:
#   export DATABASE_URL="$(aws ssm get-parameter --name /my-app/DATABASE_URL --with-decryption --query Parameter.Value --output text)"
# or supply the value through whatever credential manager you use.
# If you must create a local `.env`, replace the placeholder below with the
# real connection string.
DATABASE_URL="postgres://USER:PASSWORD@HOST:PORT/DATABASE"
